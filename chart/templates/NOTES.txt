✅ {{ include "foxbit-calc.fullname" . }} instalado (release: {{ .Release.Name }}, namespace: {{ .Release.Namespace }}).

A aplicação é exposta por um Service {{ .Values.service.type }} na porta {{ .Values.service.port }}
e é acessível SOMENTE de dentro do cluster.

Para testar a partir da sua máquina, faça um port-forward:

  kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "foxbit-calc.fullname" . }} {{ .Values.service.port }}:{{ .Values.service.port }}

E então:

  curl "http://localhost:{{ .Values.service.port }}/api/sum?term_one=4&term_two=1"
  curl "http://localhost:{{ .Values.service.port }}/api/div?term_one=4&term_two=3&precision=2"
  curl "http://localhost:{{ .Values.service.port }}/healthz"
  curl "http://localhost:{{ .Values.service.port }}/metrics"

Cache:
{{- if .Values.cache.enabled }}
  habilitado (tipo: {{ .Values.cache.type }}, TTL: {{ .Values.cache.ttl }}).
  {{- if eq .Values.cache.type "internal" }}
  Redis interno (bitnami/redis) em {{ .Release.Name }}-redis-master:6379.
  {{- else }}
  Redis externo em {{ .Values.cache.external.address }}.
  {{- end }}
{{- else }}
  desabilitado. A aplicação aplica o delay de {{ .Values.calcDelay }} em toda requisição.
{{- end }}

{{- if .Values.serviceMonitor.enabled }}
{{- if not (.Capabilities.APIVersions.Has "monitoring.coreos.com/v1") }}

⚠️  AVISO: serviceMonitor.enabled=true, mas o CRD monitoring.coreos.com/v1
   (Prometheus Operator) NÃO foi encontrado no cluster. A instalação prosseguiu
   SEM criar o ServiceMonitor. Instale o Prometheus Operator e atualize o release
   para habilitar a coleta de métricas via ServiceMonitor.
{{- else }}

📈 ServiceMonitor criado para coleta de métricas pelo Prometheus Operator.
{{- end }}
{{- end }}
